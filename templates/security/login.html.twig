{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}

    {% if app.user is not null %}

    <h3 class="mb-3 text-center"> Vous êtes déjà connecté avec l'adresse mail : {{ app.user.userIdentifier }}.
    <br>
    <a href= "{{path('app_home')}}" class="btn btn-primary m-3">Se déconnecter</a>
    </h3>

    {% else %}

<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}

        <div class="mb-3 text-center">
            Vous êtes connecté avec l'adresse mail suivante :{{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>

        </div>
    {% endif %}
    {% endif %}

        <h3 class="text-center mb-3 titre-login">Connectez-vous <br> pour rejoindre le réseau solidaire Toits Unis !</h3>

<div class="container w-80 m-3 rounded" id="login-container">

    <h1 class="h3 mb-3 font-weight-normal">Connexion</h1>
    <label for="inputEmail">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
    <label for="inputPassword">Mot de passe</label>
    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

    <input type="hidden" name="_csrf_token"
        value="{{ csrf_token('authenticate') }}"
    >

<div id="login-inscrire text-center">
        <p>Vous souhaitez proposer un hébergement solidaire ? <a href= "{{path('app_comment_ca_marche_host')}}">Contactez-nous</a></p>
        <p>vous êtes une association et vous n'avez pas encore de compte ? <a href= "{{path('app_comment_ca_marche')}}">Cliquez ici</a></p>
    </div>

        <button class="btn btn-lg m-3" id="btn-login" type="submit" id="login-button">Se connecter</button>
    
</div> 

</form>
{% endblock %}