{% extends 'base.html.twig' %}

{% block title %}Association{% endblock %}

{% block body %}
    <h1>Association</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ association.id }}</td>
            </tr>
            <tr>
                <th>NameAsso</th>
                <td>{{ association.nameAsso }}</td>
            </tr>
            <tr>
                <th>DescriptionAsso</th>
                <td>{{ association.descriptionAsso }}</td>
            </tr>
            <tr>
                <th>WebsiteUrl</th>
                <td>{{ association.websiteUrl }}</td>
            </tr>
            <tr>
                <th>ImageNameAsso</th>
                <td>{{ association.imageNameAsso }}</td>
            </tr>
            <tr>
                <th>PhoneNumberAsso</th>
                <td>{{ association.phoneNumberAsso }}</td>
            </tr>
            <tr>
                <th>EmailAsso</th>
                <td>{{ association.emailAsso }}</td>
            </tr>
            <tr>
                <th>SlugAsso</th>
                <td>{{ association.slugAsso }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_association_index') }}">back to list</a>

    <a href="{{ path('app_admin_association_edit', {'id': association.id}) }}">edit</a>

    {{ include('admin_association/_delete_form.html.twig') }}
{% endblock %}
