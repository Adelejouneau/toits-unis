<form method="post" action="{{ path('app_admin_association_delete', {'id': association.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir suprimer cette ligne?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ association.id) }}">
    <button class="btn btn-alerte"><i class="align-middle" data-feather="trash-2"></i></button>
</form>