<form method="post" action="{{ path('app_admin_department_delete', {'id': department.id}) }}" onsubmit="return confirm('Etes vous sûr(e) de vouloir supprimer ce département ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ department.id) }}">
    <button class="btn btn-danger"><i class="align-middle" data-feather="trash-2"></i></button>
</form>
