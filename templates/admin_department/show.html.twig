{% extends 'base-backend.html.twig' %}

{% block title %}Department{% endblock %}

{% block body %}
<a href="{{ path('app_admin_department_index') }}" class="btn btn-dark">Retour</a>
    <h1>Départements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ department.id }}</td>
            </tr>
            <tr>
                <th>Département</th>
                <td>{{ department.nameDepartment }}</td>
            </tr>
            <tr>
                <th>Code Département</th>
                <td>{{ department.codeDepartment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_department_edit', {'id': department.id}) }}" class="btn btn-success">Modifier</a>

    {{ include('admin_department/_delete_form.html.twig') }}
{% endblock %}
