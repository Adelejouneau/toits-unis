<form method="post" action="{{ path('app_admin_host_delete', {'id': host.id}) }}" onsubmit="return confirm('Etes vous sûr(e) de vouloir supprimer cet hôte ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ host.id) }}">
    <button class="btn btn-danger"><i class="align-middle" data-feather="trash-2"></i></button>
</form>
