{% extends 'base.html.twig' %}

{% block title %}Modifier la fiche de l'hôte{% endblock %}

{% block body %}

    <a href="{{ path('app_admin_host_index') }}" class="btn btn-dark mb-3">Retour</a>
    <div class="d-flex flex-wrap justify-content-between align-items-center">

    <h1>Modifier la fiche de l'hôte</h1>

        {{ include('admin_host/_delete_form.html.twig') }}
    </div>
    {{ include('admin_host/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
